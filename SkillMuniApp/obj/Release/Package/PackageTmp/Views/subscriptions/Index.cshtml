@model IEnumerable<m2ostcms.Models.tbl_subscriptions>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.STATUS)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UPDATED_DATE_TIME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tbl_content_org_association.STATUS)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tbl_user.IS_SOCIAL)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.STATUS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UPDATED_DATE_TIME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tbl_content_org_association.STATUS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tbl_user.IS_SOCIAL)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID_SUBSCRIPTION }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID_SUBSCRIPTION }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID_SUBSCRIPTION })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
